name: CI
on: [push, pull_request]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          node-version: 20
      - name: Install deps
        run: npm ci
      - name: Run lint
        run: npm run lint --workspaces || true
      - name: Build API
        run: npx nx build @maple-plan/api
      